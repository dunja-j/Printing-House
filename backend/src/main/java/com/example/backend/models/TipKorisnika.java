package com.example.backend.models;

/** Tip korisnickog naloga; mapira se na ENUM kolonu `korisnik.tip`. */
public enum TipKorisnika {
    klijent_fizicko,
    klijent_pravno,
    stampar,
    administrator
}
